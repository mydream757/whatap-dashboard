import React, { ReactElement } from 'react';
import { Layout } from 'antd';

export default function Dashboard(): ReactElement {
  return <Layout style={{ padding: '8px' }}>{/* Widgets */}</Layout>;
}
